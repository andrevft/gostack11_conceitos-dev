module.exports = {
    presets: [
        '@babel/preset-env',
        '@babel/preset-react' //permite escrever HTML dentro do JS
    ],
    plugins: [
        '@babel/plugin-transform-runtime'
    ]
};